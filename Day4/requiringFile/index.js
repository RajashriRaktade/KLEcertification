let  pi=Math.PI;


//arrow function
let sum = ()=>{
    return 2+3;
}
let maxNum=Math.max(2,6,8,1,10,20);

let obj ={
    name :"raja"
}

module.exports ={pi,sum,maxNum,obj}; //exporting values ot app.js