"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getBuilditJobPostings = getBuilditJobPostings;

var _jobPosting = require("./job-posting");

var _constants = require("./constants");

/**
 * Fetches all currently advertised Buildit jobs as JobPosting objects.
 *
 * Each JobPosting object will be initalised with the tracking ID for the
 * "Buildit website" source in SmartRecruiters' analytics.
 *
 * @return {Promise<Array>} Promise that resolves to an array of job postings.
 */
async function getBuilditJobPostings() {
  // eslint-disable-line import/prefer-default-export
  return (0, _jobPosting.getJobPostings)(_constants.wiproDigitalId, _constants.builditCustomFieldId, _constants.builditCustomFieldValue, _constants.builditTrId);
}